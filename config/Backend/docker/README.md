# Backend Docker

The image is built from the sibling repository Dockerfile:

`../rer-dsp-backend/Dockerfile` (path overridable via `DSP_BACKEND_PATH`).

This folder is reserved for future adopter-specific Docker overrides (entrypoint scripts, extra packages, etc.).
