# Frontend Docker

The image is built from the sibling repository Dockerfile:

`../rer-dsp-frontend/Dockerfile` (path overridable via `DSP_FRONTEND_PATH`).

This folder is reserved for future adopter-specific Docker overrides.
