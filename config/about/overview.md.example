# Overview

This is placeholder content for the "About" page. Replace this file with
text describing your platform, its purpose, and the data it publishes.

## Example section

- Use standard Markdown: headings, lists, links, and paragraphs.
- Each tab in `about-config.json` points to one `.md` file in this folder.
